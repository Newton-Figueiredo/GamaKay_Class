let test = function(){
    const name;
    let fullName = name + "" santos"";
    console.log(fullName);
    }

/*
const listaAlunos = ["aluno1","aluno2" ,"aluno3" ,"aluno4" ,"aluno5" ,"aluno6","aluno7"]

// operador spread (...)

const listaAlunos2 = [...listaAlunos,"novo-aluno"]

//console.log(listaAlunos2);

// metodo map

listaAlunos2.map(indice => console.log(indice));

// metodo filter (filtra o array com base na callback informada)

const listaNumeros = [1,2,3,4,6,56,7,8]

const resposta = listaNumeros.filter(indice => indice%2 != 0)

//console.log(resposta)

// metodo sort (ordena a lista bom base na callback passada)

console.log(listaNumeros.sort())

console.log(listaNumeros.sort((a,b)=>a-b))

console.log(listaNumeros.sort((a,b)=>b-a))

//metodo reduce executa uma função reducer (fornecida por você) para cada elemento do array, resultando num único valor de retorno.

const somador = listaNumeros.reduce((indice1,indice2) => indice1+indice2,0)
console.log(somador)


*/